apiVersion: v1
kind: Service
metadata:
  name: user-management-loadbalancer
spec:
  selector:
    app: user-management
  ports:
    - name: user-management
      port: 8090
      targetPort: 8080
  type: LoadBalancer